@page
@model ge_repository.Pages.Transform.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>transform</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.transform.project.name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.transform.project.name)
        </dd>
        <partial name="_DetailsTransform"
                    for="transform"
                    view-data="ViewData" />   
        <partial name="_DetailsBase"
                    for="transform"
                    view-data="ViewData" />
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.transform.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
