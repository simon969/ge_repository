@page "{id:Guid}"
@model ge_repository.Pages.Data.DeleteModel

@{
    ViewData["Title"] = "Delete GE Data";
}

<h2>Delete GE Data</h2>

<h3>Are you sure you want to delete this file?</h3>
<div>
     <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.data.project.name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.data.project.name)
        </dd>
         <partial name="_DetailsData"
                    for="data"
                    view-data="ViewData" />   
        <partial name="_DetailsLocation"
                    for="data"
                    view-data="ViewData" />
        <partial name="_DetailsBase"
                    for="data"
                    view-data="ViewData" />
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="data.Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-page="./Index" asp-route-projectId="@(Model.data.projectId)">Back to List</a>
    </form>
</div>